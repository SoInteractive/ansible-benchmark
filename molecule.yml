---
ansible:
  playbook: tests/playbook.yml
driver:
  name: docker
verifier:
  name: testinfra
docker:
  containers:
    - name: benchmark-xenial
      image: ubuntu
      image_version: xenial
#    - name: benchmark-centos
#      image: centos
#      image_version: latest
